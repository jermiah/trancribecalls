# Required for client-side uploads to Vercel Blob (direct browser → Blob).
# Create a read-write token in the Vercel dashboard: Storage → Blob → .env.local
BLOB_READ_WRITE_TOKEN=

# Optional: increase visibility during local debugging (never enable in production)
# DEBUG=
